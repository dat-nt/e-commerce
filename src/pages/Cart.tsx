import { Typography } from "antd";

const { Title } = Typography;

function Cart() {
    return (
        <Typography>
            <Title>Cart</Title>
        </Typography>
    );
}

export default Cart;