import { Typography } from "antd";

const { Title } = Typography;

function AddProduct() {
    return (
        <Typography>
            <Title>Add Product</Title>
        </Typography>
    );
}

export default AddProduct;